# LangGraph and LangChain dependencies
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0  # For OpenAI models
openai>=1.44.0
langchain-anthropic>=0.2.0  # For Anthropic models (optional)
langchain-community>=0.3.0  # For SQL database tools

# Database
sqlalchemy>=2.0.0  # For SQL database operations

# API and HTTP
requests>=2.31.0
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0
sse-starlette>=1.6.0  # For Server-Sent Events (streaming progress updates)
folium>=0.15.0
pandas>=2.0.0
beautifulsoup4>=4.12.0  # For web scraping of review content
rich>=13.7.0  # For terminal UI rendering in review simulation mode

# Python typing extensions
typing-extensions>=4.8.0

# Environment variables
python-dotenv>=1.0.0

# Configuration and templating
PyYAML>=6.0.0  # For YAML configuration files
Jinja2>=3.1.0  # For prompt templating

# Search and ranking
rank-bm25>=0.2.2  # For BM25 sparse keyword ranking
sentence-transformers>=2.2.0  # For dense embedding generation (all-mpnet-base-v2)
faiss-cpu>=1.7.4  # For FAISS vector similarity search (use faiss-gpu for GPU support)
numpy>=1.24.0  # For numerical operations

import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "IDSS Vehicle Search | Stanford LDR Lab",
  description: "Interactive Decision Support System for vehicle search and recommendations",
  keywords: ["vehicle search", "car shopping", "AI assistant", "decision support", "Stanford"],
  applicationName: "Vehicle Search Agent",
  authors: [{ name: "Stanford LDR Lab" }],
  openGraph: {
    title: "Vehicle Search",
    description: "AI-powered vehicle search and recommendation system",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
